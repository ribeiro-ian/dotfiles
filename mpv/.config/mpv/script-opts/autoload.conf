# Only load video files (not images/audio)
images=no
videos=yes
audio=no

# Ignore files starting with '.' (hidden files)
ignore_hidden=yes

# Only load files of the same type as the current file
same_type=yes

# Try to detect if files belong to the same series (helps avoid mixing different shows)
same_series=yes
